# Used only for the one-time bootstrap of the Terraform state bucket (backend/).
# After initial setup, CI/CD uses Workload Identity Federation — no key file needed.
# Note: path must be absolute!
export GOOGLE_APPLICATION_CREDENTIALS="<absolute_path_to_service_account>.json"
